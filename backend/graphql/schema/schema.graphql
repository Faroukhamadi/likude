scalar Time

type User {
	id: ID!
	username: String
	password: String
	karma: Float
	createdAt: Time
	updatedAt: Time
}

input CreateUserInput {
	username: String!
	password: String!
}

type Mutation {
	createUser(input: CreateUserInput!): User!
}

type Query {
	users: [User!]
}
